cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
  - node_modules/
  - .npm/
  
before_script:
  - npm i --cache .npm --prefer-offline

lint:
  stage: test
  tags:
  - nodejs
  script:
  - npm run lint

audit:
  stage: test
  allow_failure: true
  tags:
  - nodejs
  script:
  - npm audit