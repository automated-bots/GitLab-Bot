before_script:
  - npm install

cache:
  key: "$CI_COMMIT_REF_SLUG"
  untracked: true
  paths:
  - node_modules

lint:
  stage: test
  tags:
  - nodejs
  script:
  - npm run lint

audit:
  stage: test
  tags:
  - nodejs
  script:
  - npm audit